/**
 * Apple Sign In: add passport-apple or custom strategy when
 * APPLE_CLIENT_ID, APPLE_TEAM_ID, APPLE_KEY_ID, APPLE_PRIVATE_KEY are set.
 * Apple routes in auth.controller currently return 501 until configured.
 */
export const AppleStrategyPlaceholder = null;
